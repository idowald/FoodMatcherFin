# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'FoodMatcher' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod ‘Parse'
pod 'GoogleMaps'
pod 'Koloda', '~> 3.1.1'
pod 'pop', '~> 1.0'

  # Pods for FoodMatcher

  target 'FoodMatcherTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FoodMatcherUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
